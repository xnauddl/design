{
  "name": "design-system-linker",
  "version": "0.1.0",
  "private": true,
  "description": "Figma 플러그인 — 디자인 시스템(토큰) 생성 · 레이어 리네임 · 변수 연결",
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
