{
  "name": "design-system-linker",
  "version": "0.1.0",
  "private": true,
  "description": "Figma 플러그인 — 디자인 시스템(토큰) 생성 · 레이어 리네임 · 변수 연결",
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "build:dev": "node build.mjs --dev",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test",
    "verify:lists": "npm run build && node scripts/verify-lists.mjs && node scripts/verify-lists.mjs --width 360"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
