# Design System Linker — 로드맵

> 이 파일은 Notion 로드맵 페이지의 **단일 소스**입니다. `main`에 푸시하면 GitHub Action이
> Notion 페이지를 체크박스 목록으로 자동 동기화합니다. 상세 설계는 `README.md` 참고.

## 범례
- `[x]` 구현 완료 — 별도 표기 없으면 **main 병합**. `(PR #N)`는 브랜치 구현 완료·**병합 대기**.
- `[ ]` 예정.

## 1. 핵심 기능
- [x] **토큰 추출** — 선택에서 색·타이포·간격·반경·효과 후보 추출
- [x] **3계층 변수 생성** — Global(원시) → Semantic(별칭) → Component, 멱등 upsert
- [x] **시맨틱 매핑** — 역할(surface/text/…) → Global 별칭
- [x] **비색상 시맨틱 추천** — 간격·반경 티셔츠 스케일 자동 추천·병합 `(PR #31)`
- [x] **자동 바인딩** — 가장 가까운 변수에 연결(허용오차) · **용도 스코프 정확 매칭**(여백·크기·반경·선두께·텍스트·불투명도가 서로 오매칭되지 않게 `scopes`로 제한; 색상도 fill/stroke/effect 분리) · 커버리지: 색·크기·여백(gap/padding)·반경·**선두께(border)**·**레이어 불투명도**·폰트크기/행간/자간/**패밀리** `(PR #47~50)`
- [x] **레이어 리네임(역할 기반 재설계)** — 시맨틱 역할 추론·숫자 제거·맥락 정제, diff 미리보기
- [x] **브랜드 팔레트 생성** — OKLCH 톤 스케일·하모니·중립·상태색
- [x] **코드 내보내기** — W3C 디자인 토큰 JSON / CSS 변수 (px·rem)
- [x] **텍스트 스타일** — 화면 텍스트 스캔 → 시맨틱 변수 → 텍스트 스타일 등록·바인딩(+원본 적용)(`textStyles.ts`)
- [x] **시스템화 마법사** — 추출→생성→바인딩→정돈(리네임) **원클릭** 파이프라인(`wizard.ts`)

## 2. 컴포넌트 / 베리언트 (Paid)
- [x] **Phase 3 — 컴포넌트 등록 + 베리언트 분류** (속성=값 추론, `combineAsVariants`)
- [x] **Phase 4 — 누락 조합 자동 생성** (2D 그리드 정렬·리사이즈)
- [x] **Phase 4.1 — 컴포넌트 속성 노출** (TEXT / INSTANCE_SWAP / BOOLEAN)
- [ ] **라이브러리 발행** — Figma Plugin API 미지원 → 수동/조직 정책

## 3. UI / 메뉴 개편
- [x] **탭 3개 재편** — 만들기 / 적용 / 관리 (sticky 탭바)
- [x] **패널 톤앤매너 정렬** — 목업 기준 시각 정리
- [x] **시스템화 마법사 진행 안내** — 단계 정의·순서·읽기/쓰기 구분(`wizard.ts`)
- [x] **유료 게이팅 노출** — 유료(Paid) 카드 🔒 배지·비활성
- [x] **단계별 의존관계 시각화** — 시작 탭 ‘진행 안내’ 파이프라인(`pipeline.ts`): 토큰 생성→시맨틱→바인딩 단계 상태(완료/준비됨/전제 미충족) + 클릭 이동(`PREREQ_STATE` 연동)

## 4. UX 개선
- [x] **UX1~UX8** — 미리보기·단일 Undo·사유별 스킵·빈 상태·선택 동기화·진행률/취소·오류 복구·접근성(키보드)
- [x] **대량 선택 점진 렌더** — 토큰 목록·선택 트리·색 편집표를 프레임 단위 청크로 비차단 렌더(`renderChunked`, 소량은 즉시) `(PR #42)`
- [x] **i18n 인프라 + 상태/피드백 외부화** — `i18n.ts`(`t()`·`STRINGS`·`{var}` 보간) + ui.ts의 `setStatus` 피드백 메시지 전부 키로 이전 `(PR #43)`
- [x] **정적 라벨·마법사 문구·라벨 맵 외부화** — ui.html 정적 라벨 100개를 `data-i18n`/`data-i18n-html` + `applyStaticLabels()`로, 마법사 단계·건너뜀 사유·스킵 사유 맵을 `t()` 키로 이전 `(PR #70)`
- [ ] **잔여** — `title` 툴팁·`<option>`·`placeholder` 외부화(같은 `t()` 패턴)

## 5. 유료화 / 상용 전환 — Free/Paid 2티어
- **모델**: 오픈 코어 + Freemium. **Free / Paid 2티어** · 연 $39 · LemonSqueezy(웹 결제 → 키 발급 → 무료 서버리스 검증).
- **게이팅**: **기능 기반**(회당 횟수 한도 없음). 미리보기는 Free, 생성·등록은 Paid.

### 티어 경계 (기능 게이팅)
경계선은 **기존 자산을 다루는 일 / 새 자산을 만드는 일**.
- **Free(무제한)**: 추출 · 바인딩 · 리네임 · 닮은 프레임 스캔 · 코드 내보내기
- **Paid**: 위 전부 + 팔레트 생성/적용 · 토큰(3계층 변수) 생성 · 시맨틱 매핑 · 컴포넌트/베리언트(Phase 3/4/4.1) · 닮은 프레임 컴포넌트화 · 텍스트 스타일 · 다크 테마 생성
- 유료 카드는 **미리보기까지 함께** 잠근다(미리보기만 열면 적용이 회색이라 막다른 길). 읽기 전용이고 결과가 그 자체로 완결되는 닮은 프레임 '스캔'만 예외.

### 가격
- **Paid** — 연 $39(≈$3.25/월) 단일. (월 옵션·런치 프로모는 추후) LemonSqueezy 구독, License keys · activation limit=1 → 기기 1대.

### 결제 / 검증 아키텍처 (방식 C — 무료 서버리스)
- **결제: LemonSqueezy**(MoR, 5%+$0.50) — 로그인 없는 **키 기반**, **기기 1대**(activation limit), 구독·기기 관리는 LS Customer Portal 위임.
- **검증: Cloudflare Worker**(`workers/verify`) — LS 키 activate/validate → **ES256 서명 JWT** 발급. 플러그인은 공개키로 검증·캐시(오프라인 grace 14일). 키 해제 시 활성화 슬롯 반납(best-effort). 고정비 ~$0(서버리스·DB 없음), 변동비는 결제 수수료뿐.

### 보안 / 관리
- [x] **개발용 티어 토글을 `__DEV__` 빌드 플래그로 dev 빌드 전용화**(배포 빌드 백도어 차단)
- [x] **오너 관리자 키**(Worker allowlist) — 결제 없이 paid 토큰(스모크 테스트)
- [x] **검증 페일오픈 차단** — fetch 자체 실패만 `offline`(grace 유지). 서명 불일치·공개키 오설정·5xx·비JSON 응답은 검증 실패로 확정 `(PR #71)`
- [x] **서명 없는 평문 응답 거부** — `token` 없는 응답을 수용하던 폴백 제거(배포 빌드에도 포함돼 M2.1과 모순이었음) `(PR #71)`
- [x] **구매·구독 관리 링크 UI** — `OPEN_LICENSE_LINK` → code의 `figma.openExternal`. URL은 code가 `licenseConfig`에서 해석(임의 URL 전달 금지) `(PR #71)`

### 출시 단계
- [x] **M1 — 엔타이틀먼트(기능 게이팅) + 개발용 티어 토글 + 백도어 차단**
- [x] **M2 — 라이선스 키 입력·검증 + 캐시·오프라인 grace**
- [x] **M2.1 — 서명 토큰(JWT) 검증 코어** (alg=none 거부)
- [x] **M2.2 — 검증을 UI 아이프레임으로 이동(WebCrypto)**
- [x] ~~**M3 — 공유 프리셋(Paid)**~~ _(v2 재설계에서 제거 — 아래 §8.5. 변경 이력도 제거 — PR #37)_
- [x] **C — Cloudflare Worker 검증기 + LemonSqueezy 연동 + 기기 1대(activation limit) + 해제 시 슬롯 반납**
- [ ] **배포 — 실제 도메인·공개키·결제/포털 링크 교체**

## 6. 배포 전 남은 일
- [ ] `licenseConfig.ts` `VERIFY_URL`·공개키·`PURCHASE_URL`·`PORTAL_URL`, `manifest.json` `allowedDomains` 실제 값 교체 (가이드: `workers/verify/README.md`)
- [ ] 검증 서버(Cloudflare Worker) 배포 · LemonSqueezy 스토어/제품 설정(License keys, activation limit=1)
- [x] 결제 제공자 선택(**LemonSqueezy**) · 검증 서버 코드(**Cloudflare Worker**) 작성 — *배포만 남음*
- [x] 가격 확정 — **연 $39**
- [x] **빌드 타깃 es2017** — Figma 샌드박스 객체 스프레드 호환
- [x] **플러그인 산출물(dist) 커밋** — 브랜치/main에서 직접 로드 가능
- [ ] Figma 정책(외부 결제/키 방식) 최종 확인
- [ ] Figma Community 게시 (아이콘·커버·캐러셀·소개글 — `store/figma-listing.md`)

## 7. 프라이버시
- 토큰·디자인 데이터는 **로컬 처리**. 외부 전송은 **라이선스 검증 요청(키 + pluginId)** 에 한정 — 디자인 데이터 미전송.

## 8. v2 재설계 — 결정 완료·구현 대기 (상세: `REDESIGN.md`)
> 확정된 방향. 엔진(`lib/` 순수 함수)은 대부분 유지, **UI/IA · 결과 표면화 · 색 계층 정렬** 중심. 우선순위 상위부터 점진.

### 8.1 미리보기·적용 — 선택형 레이어 트리
- [x] 공통 **미리보기→선택→적용 트리** 컴포넌트 (전체 서브트리 + 영향 노드 강조) `(PR #37)`
- [x] 리네임 트리 미리보기 (파일럿) `(PR #37)`
- [x] 바인딩 트리 미리보기 + **WYSIWYG 직접 적용**(`APPLY_SELECTED`) `(PR #37)`
- [x] 컴포넌트 등록 — 선택 디자인 **하위에서 골라** 등록(`SCAN_COMPONENT_CANDIDATES`) `(PR #37)`

### 8.2 리네임 규칙 보강
- [x] 루트(최종) 프레임 **항상 보존**(기본명 포함) `(PR #37)`
- [x] 인스턴스 **서브트리까지 스킵** (기존 보존: 컴포넌트·텍스트·잠금·명명 레이어) `(PR #37)`

### 8.3 색·토큰 계층 정렬 — Global=primitive / Semantic=role
- [x] 팔레트 Global을 **hue로 통일**(`color/blue/500`), 역할은 Semantic만(`paletteSemanticMap`) `(PR #37)`
- [x] 동일 hue 충돌 **접미사 인덱스**(`blue`, `blue-2`) `(PR #37)`
- [x] 시맨틱 매핑 **일반화**(`suggestSemanticMap(colors)`) — 추출 색에서도 역할 추천(실제 이름 지시) `(PR #37)`
- [x] 추출 색 **편집표**(hue·역할 표시·역할 확정·반영) + **추출 Global hue 버킷팅**(`nameColorsByHue`, 충돌 접미사) `(PR #37)`
- [x] **기존 Global 스캔**(재방문 매핑) — `GET_GLOBAL_COLORS`로 기존 Global 색을 읽어 `suggestSemanticMap` 추천(‘기존 색에서 추천’ 버튼) `(PR #39)`
- [x] 전 토큰 역할 어휘 확장 — `suggestTokenRoles`(순수): spacing/radius/size 센터 티셔츠, fontSize type 스케일, fontWeight 이름, fontFamily 키워드/순서. (opacity·letterSpacing 약함 / effects 후순위 제외) `(PR #40)`
- [x] **단위 토큰 단일화** — 비-px lineHeight/letterSpacing을 px FLOAT 단일로(스냅샷 폐기) + 원본 단위는 `Variable.description`("160%"), 내보내기 우선(`unitDescription`) `(PR #38)`

### 8.4 IA · UX
- [x] 시스템화 마법사 **별도 탭(시작)** `(PR #37)`
- [x] **4탭 재편**(시작·만들기·적용·관리, 색은 만들기 안) `(PR #37)`
- [x] 판넬 **크기 확대 + 리사이즈**(우하단 핸들·`clientStorage` 기억) `(PR #37)`
- [x] 불필요 **텍스트·불릿 정리**(전제 안내를 게이트로 대체, 산문 축약) `(PR #37)`
- [x] **통합 게이트** — 전제 미충족(Global/바인딩 변수) 비활성+안내+바로가기 + 유료 🔒를 `updateGates` 한 메커니즘으로(`PREREQ_STATE`) `(PR #37)`

### 8.5 신규 · 정리
- [x] 명도 대비 **후속 보정**(텍스트색 기본 + 배경 옵션) — `suggestContrastFix`(OKLCH L 이분 탐색) + 행별 ‘텍스트/배경’ 적용(`APPLY_CONTRAST_FIX`) `(PR #37)` → **철회**: 대비 점검 자체를 제품 범위에서 뺐다(아래)
- [x] **명도 대비 점검 제거** — 카드·마법사 단계(`접근성 검수`)·메시지(`CHECK_CONTRAST`/`CONTRAST_RESULT`)·`lib/contrast.ts`를 전부 삭제. 접근성 검사는 전용 플러그인의 영역이라 비목표로 확정
- [x] **변경 이력 제거** — `history.ts`·이력 카드·메시지·기록 호출부 전면 삭제 `(PR #37)`
- [x] **Free 마법사 완주** — `create`·`semantics`를 Paid 단계로 표시해 Free에서는 계획에서 건너뛴다(전엔 실행 → `PREMIUM_REQUIRED`로 2단계에서 시퀀스 중단). Free 마법사 = 추출 → 바인딩 → 리네임. 실패 사유는 칩 툴팁뿐 아니라 요약 줄에도 싣는다
- [x] **구조 단계 한 카드로** — 컴포넌트/베리언트 카드와 닮은 프레임 카드를 「구조」 한 장으로. 툴바(후보 스캔·닮은 스캔 / 컴포넌트 등록·컴포넌트화) + 목록은 그룹 머리(컴포넌트 후보 · 닮은 프레임)로 구분, 상태 줄도 하나로(`structureStatus`)
- [x] **보조색·하모니는 유지(와이어와 의도적 불일치)** — 와이어의 색 카드 시드 줄에는 브랜드색·중립·상태만 있고 「보조색」 체크박스와 「하모니」 선택은 없다. 지우지 않기로 확정했다. 하모니는 `generatePalette`가 보조색을 각도로 도출하는 유일한 입구고(`useBrand2` 미체크면 둘 다 미적용), 브랜드가 2색인 팀에는 팔레트 생성의 핵심 입력이다. 와이어가 시드 줄을 한 줄로 줄여 그린 결과지 기능을 뺀 것이 아니다 — **와이어 대조 시 이 줄은 차이로 세지 않는다**
- [x] **문구 전면 대조** — 판넬 9화면(마법사 · 만들기 4 · 적용 3 · 관리)의 제목·hint·버튼을 와이어와 한 줄씩 맞췄다. 「적용 (바인딩)」→「바인딩」, 「다크 테마 생성」→「다크 테마」, 「다크 채우기」→「다크 생성」, 「대상 컬렉션/라이트 모드/다크 모드」→「컬렉션/라이트/다크」, 「전체 선택」→「전체」, 구조·설정 hint를 와이어 문장으로. 잠금 배지는 「🔒 Paid 전용」→ 자물쇠 하나(사유는 툴팁)로 하되, Free와 Paid가 한 툴바에 섞인 색 카드는 「팔레트 생성」 버튼에도 자물쇠를 붙이고 카드 제목에 범위 라벨(「추출 → 변수 · 팔레트」·「컴포넌트 · 닮은 프레임」)을 넣었다. 와이어에 있는데 없던 색 목록 머리줄과 라이선스 요금 줄을 신설(요금은 배포 전에도 표시 — 링크와 무관한 사실). 다크 카드 CTA가 셀렉트 사이에 끼어 있던 것도 툴바로 올렸다. 상태 메시지의 「…없어요/…찾아드려요」 말투 14곳은 와이어와 같은 「…합니다/…하세요」로 통일
- [x] **창 크기 400×660 고정(#14) — 리사이즈 없음** — 가로는 와이어가 400으로 못 박았고(가로로 늘려 표를 버티는 패턴은 #17에서 비채택), 세로 드래그 리사이즈는 붙였다가 **철회**했다. 플러그인 iframe에서는 포인터 캡처가 iframe 경계를 넘지 못해 아래로 끌면 `pointermove`가 끊기고, 창을 포인터보다 크게 따라가게 하는 우회(`max(startH+ΔY, y+10)`)까지 넣어도 실기에서 멎었다. 손잡이 크기·저장 경로(트레일링 쓰기)도 함께 시도했으나 해결되지 않았다. 크기를 고정하면 모든 화면이 한 크기에서만 성립하면 되고 검증도 거기서 끝난다 — 목록이 길면 패널 body가 스크롤한다(#18). `RESIZE` 메시지·`#resizeHandle`·`clientStorage` 크기 저장을 전부 제거
- [x] **관리 탭 = 와이어 3장** — 카드 제목을 와이어대로(「변수 → 코드」·「라이선스」), 내보내기 CTA를 다른 카드와 같은 **툴바 오른쪽 끝**으로(형식·단위 셀렉트는 값이 곧 라벨이라 머리말 제거·툴팁 보완), 허용오차를 회색 문장에서 **비활성 필드 + 「고정」**으로(기준 크기·깊이와 같은 문법이라야 정해진 값이 보인다), 라이선스 안내를 한 줄로(#15). 와이어에 없던 「접근성 · 국제화」 정적 블록은 제거 — 개발 메모지 사용자 화면의 정보가 아니다. 「해제」 버튼은 와이어에 없어도 남긴다(기기 1대 한도라 이게 없으면 기기를 못 옮긴다)
- [x] **구조 툴바 = 와이어 4개** — 툴바에 여섯 개를 늘어놓던 것을 와이어와 같은 **스캔 2 + 쓰기 2**로. 빠진 「베리언트 분류」·「누락 조합 생성」은 기능을 지우지 않고(Phase 3/4는 Paid 판매 항목) **조건부 노출**로 옮겼다 — 세트에 안 속한 낱개 `COMPONENT`나 `COMPONENT_SET`을 실제로 선택했을 때만 결과 목록 아래 「선택한 컴포넌트 정리」 줄로 나타난다(`SELECTION_STATE.comps`/`sets`). 스캔→등록이라는 카드의 주 흐름과, 이미 만든 세트를 손보는 뒷일을 분리한 것
- [x] **색 → 변수(#3·#10 UI)** — 팔레트 카드와 추출·색 정리 카드를 **「색」 한 장**으로 합치고, 결론 버튼 「색 변수 만들기」 신설: 역할 입력 → 시맨틱 매핑 반영 → 색 Global(hue) 생성 → 결과 도착 시 Semantic 별칭까지 이어짓기. 팔레트 「적용(변수 생성)」·「시맨틱 매핑에 반영」 두 버튼은 여기에 흡수. 토큰 단계는 **색 외만** 만들고(`tokensToCreate`) 상단에 '색 변수 N개 이미 생성됨' 배너
- [x] **색 목록 = 전부 나열(와이어와 의도적 불일치)** — 와이어의 색 카드는 결과를 5행만 그려 두고 상태줄엔 「색 12개」라, 목록이 대표색만 보여 주는 것처럼 읽힌다. **전부 나열로 확정**(사용자 결정). 어느 색에 역할을 줄지 고르려면 후보가 다 보여야 하고, 추려서 보여 주면 그 규칙(무엇이 대표인가)이 또 하나의 숨은 판정이 된다. 팔레트 한 벌은 66행이 되므로 대신 **`palette-row` 스와치 한 줄**로 램프를 한눈에 보게 하고, 역할은 행마다 드롭다운(`주색 · primary`)으로 고른다 — **와이어 대조 시 목록 길이는 차이로 세지 않는다**
- [x] **마법사 온보딩 흡수** — 별도 '온보딩/가이드' 카드를 없애고 마법사 탭 첫 줄 배너로(‘다시 보지 않기’는 `clientStorage`에 기억). 실행 단계는 세로 목록에서 **미니 파이프 칩**(돌 단계만 번호순, 결과·사유는 툴팁)으로, '진행 안내' 카드는 **'진행'** 컴팩트 스트립으로
- [x] **긴 hint 제거(#15)** — 카드 안 산문을 와이어 한 줄로(다크 5줄→1, 컴포넌트 10줄+보조줄→1, 색 정리·토큰·닮은 프레임·내보내기·마법사 동일). 동작을 바꾸는 상세(후보 자격 규칙·다크 대상 규칙·행 라벨 규칙)는 툴팁으로 옮김. 와이어에 있는데 없던 바인딩·리네임 한 줄은 신설
- [x] **텍스트 스타일 = 리스트 + ×N(#17)** — 7열 표(이름·폰트·크기·행간·자간·스타일·삭제)를 폐기하고 `r-card` 리스트로: 이름(편집·파랑=등록됨/앰버=신규) · 요약 한 줄(`Inter · 32 / 120% / 0 · Bold`) · `×N` 배지. 배지를 누르면 그 글자들을 캔버스에서 선택(1개면 이동) — `StyleCluster.nodeIds`·`TextStyleSpec.count/nodeIds`를 순수 계층에서 날라 `SELECT_NODES`로
- [x] **CTA = 카드 상단 툴바** — 미리보기·적용·다음을 제목 바로 아래 한 줄로(구분선). 주 버튼을 타이틀 줄로 옮기던 캐논 패턴(`TITLE_BTN_IDS`)은 툴바로 대체. 단계 레일과 같은 이동을 카드 안에서 하는 `다음 →`(색→토큰→테마→타이포 / 바인딩→리네임→구조) 추가. 텍스트 스타일의 '등록'·'기존 스타일 적용만'도 한 툴바로 모음
- [x] **결과 목록·행 통일(#18)** — 목록별 스크롤 박스(`list-region`·행 높이 스냅·‘모두 펼치기’) 제거 → 패널 body 스크롤. 행은 `.tk`·`.crow`·`.tree-row`·`.simrow`·`.vr-row` 다섯 문법을 `r-card`(체크·제목·요약·메타) 하나로. 트리는 그룹 머리 + 자식 카드. `scripts/verify-lists.mjs`는 상한이 만들던 ‘반쪽 행’을 지키던 하네스라 함께 삭제
- [x] **단계 레일** — 만들기 4단계(색·토큰·테마·타이포) · 적용 3단계(바인딩·리네임·구조), 한 화면에 한 단계. 바로가기(`goToCreate`·`gotoStep`)도 단계까지 맞춤
- [x] **설정을 관리 탭으로** — 「플러그인 설정」 카드 신설(기준 크기·리네임 맥락 깊이) + `clientStorage` 자동 저장(`GET/SET_SETTINGS`). 허용오차 칩·입력 제거(0.5 고정), 사다리·허용 입력 제거(격자 8·15% 고정, 미리보기에서 자동 스냅)
- [x] **변수 편집기 제거** — 카드·행 편집 UI·`lib/variableEdit.ts`·`EDIT_VARIABLE`/`DELETE_VARIABLE`/`GET_VARIABLE_USAGE`·문서 전체 사용처 스캔을 삭제. 변수 값·이름 수정은 Figma Variables 패널의 일이고, 플러그인은 만들기(upsert)·적용(바인딩)이 몫. `GET_VARIABLES`는 다크 테마 카드가 컬렉션·모드를 고르는 데 필요해 남기되 `VarInfo`를 목록용으로 축소(값·스코프 제거), 시작 시 1회 요청으로 배선
- [x] **공유 프리셋 제거** — `presets.ts`·프리셋 카드·`GET/SAVE/DELETE_PRESET`·`PRESETS`·`presets` 엔타이틀먼트·`dsl.presets` 저장을 전면 삭제. base·허용오차·맥락단계는 관리 탭에서 `clientStorage` 자동 유지로 대체(설정 자동 유지는 IA 셸 작업). 시맨틱 매핑 텍스트 변환(`semanticMapToText`/`textToSemanticMap`)은 매핑 UI가 쓰므로 `roles.ts`로 이동
- [x] 내보내기 미리보기 **비목표 확정**(현행 유지 — 코드 변경 없음) `(PR #37)`
- [x] **내보내기 1버튼화** — 결과 textarea와 '다운로드' 버튼(이원화)을 없애고 「내보내기」가 곧바로 `tokens.json`/`tokens.css`를 저장. 변수가 없으면 껍데기 파일 대신 안내(`exportHasTokens`, 순수·테스트)
- [x] **격자·허용오차는 관리 탭에서 수정(#19 결정 수정 2026-08-07)** — 앞선 결정(격자 8 고정 · 오차 0.5 고정 표시)이 서로 아귀가 안 맞았다. 격자 8로 옮긴 값은 원래 레이어와 최대 4px 어긋나는데 오차가 0.5면 그 레이어는 연결에서 조용히 빠진다 — 스냅은 성공하고 실패는 두 단계 뒤에 나타난다. **두 값 모두 관리 탭에서만 고칠 수 있게** 하고(만들기·적용 화면에는 여전히 없음) `clientStorage`에 함께 보관한다(`SETTINGS.grid`/`tolerance`). 기본값은 8·0.5 그대로 — 오차를 기본으로 넓히면 격자에 안 맞는 폰트 크기·반경까지 과매칭되기 때문. 대신 **실제로 옮긴 값이 있고 오차가 격자의 절반보다 좁을 때만** 토큰 정리 요약 뒤에 그 사실을 붙인다(설정 카드에 상시 경고를 두면 기본값에서 늘 떠 벽지가 된다)
- [x] **Paid로 빠진 단계는 회색 자물쇠 칩으로 남긴다** — 미니 파이프가 '돌 단계만' 그리도록 바뀌면서 Free 사용자는 파이프가 왜 세 칸만 도는지 알 수 없었다(칩 3개 → 변수가 없어 연결 0 → 화면 어디에도 '토큰 생성은 Paid'라는 말 없음). **옵션을 꺼서 빠진 단계는 그대로 칩 없음**(바로 위 체크박스가 이미 말한다), **Paid로 빠진 단계만** `🔒 토큰` 회색 칩으로 자리를 남기고 요약 줄에도 인과를 싣는다(「Paid 전용이라 건너뜀: 토큰 — 새 변수를 만들지 않았으니 이미 있는 변수에만 연결합니다」, 상태는 warn). 이를 위해 `planWizard`가 선택 단계에서 **티어보다 옵션을 먼저** 본다 — 안 켠 단계까지 'Paid 전용'으로 적으면 살 생각도 없던 기능이 잠금으로 세어져 파이프가 자물쇠로 뒤덮인다
- [x] **텍스트 스타일 값 입력 = 수동·폴백 행만** — 스캔 행은 캔버스에서 읽은 사실이라 이름만 고친다(값을 고치면 화면과 스타일이 어긋난다). 반면 「행 추가」로 만든 행과 텍스트를 못 찾아 채운 기본 램프는 근거로 삼을 글자가 없는데도 Inter/16/24/0에 못 박혀 있었고, 안내는 「폰트·값을 조정하세요」라고 말했다. `boundStyleId`도 `count`도 없는 행에만 값 입력 줄(`폰트 · 크기 / 행간 / 자간 · 굵기`)을 연다 — 칸 순서는 스캔 행 요약과 같게 둬 같은 목록에서 눈이 옮겨 다니게
